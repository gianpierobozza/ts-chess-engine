import { expect } from 'chai';

describe('Tile', () => {
    describe('when asking to create a new one', () => {
        it('checking default options', () => {
            expect(1).to.be.equal(1);
        });
    });
});
